<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
    <div class="container-fluid">
        <a class="navbar-brand" href="/home"><img class="img-fluid" src="images/Logo.svg" width="70" height="20px" alt="logo" /></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
            aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        
            <div class="collapse navbar-collapse" id="navbarNav">
                <li class="nav-item"><a class="text-white linkMenu" href="/">Inicio</a></li>
                <ul class="navbar-nav">
                    <% if (torneos.length> 0) { %>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdown navbarDropdown2"
                            role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Torneos
                        </a>
                        <ul class="dropdown-menu dropdown-menu2" aria-labelledby="navbarDropdown">
                            <% torneos.forEach((torneos)=>{ %>
                                <li><a class="dropdown-item linkMenu" href="/torneos<%= torneos.codTorneo %>">
                                        <%= torneos.nombreTorneo %>
                                    </a></li>
                                <% }) %>
                        </ul>
                    </li>
                    <% } %>
                
                    <% if (deportes.length> 0) { %>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdown navbarDropdown2"
                            role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Ranking
                        </a>
                        <ul class="dropdown-menu dropdown-menu2" aria-labelledby="navbarDropdown">
                            <% deportes.forEach((deportes)=>{ %>
                                <li><a class="dropdown-item linkMenu" href="/ranking<%= deportes.id %><%= deportes.codTorneo %>">
                                        <%= deportes.nombreDeporte %>
                                    </a></li>
                                <% }) %>
                        </ul>
                    </li>
                    <% } %>
                    <% if (deportesConEquipos.length> 0) { %>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle text-white" href="#" id="navbarDropdown navbarDropdown2"
                            role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Equipos
                        </a>
                        <ul class="dropdown-menu dropdown-menu2" aria-labelledby="navbarDropdown">
                            <% deportesConEquipos.forEach((equipos)=>{ %>
                                <li><a class="dropdown-item linkMenu" href="/equipos<%= equipos.id %>">
                                        <%= equipos.nombreDeporte %>
                                    </a></li>
                                <% }) %>
                        </ul>
                    </li>
                    <% } %>
                   
                </ul>
            </div>
    </div>
</nav>
